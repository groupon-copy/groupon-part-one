<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".One.MainFeedSearchActivity">

    <include
        android:id="@+id/mainFeedSearchActivity_toolbar"
        layout="@layout/tool_bar_test" />

    <LinearLayout
        android:id="@+id/mainFeedSearchActivity_LL"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:layout_above="@+id/mainFeedSearchActivity_LLButton"
        android:layout_below="@+id/mainFeedSearchActivity_toolbar">

        <!-- this is where the mainFeedDealListLayoutFragment will be placed -->

    </LinearLayout>

    <LinearLayout
        android:id="@+id/mainFeedSearchActivity_LLButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="5dp"
        android:background="@color/colorPrimary"
        android:layout_alignParentBottom="true">

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="20dp"
            android:text="@string/main_screen"
            android:background="@drawable/custom_button"
            android:gravity="center"
            android:textColor="@android:color/white"
            android:onClick="gotoMainScreenActivity"/>

    </LinearLayout>

</RelativeLayout>
